---
import FAQSection from './FAQSection';
import faq from '../../data/faq.json';

interface Props {
  limit?: number;
}

const { limit } = Astro.props;
---

<FAQSection client:visible items={faq} limit={limit} />
